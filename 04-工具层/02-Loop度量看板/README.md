# Loop 度量看板

> **版本**：v1.0 / 2026-06-30
> **用途**：38 条 KPI 多项目横向对比 + 时间趋势追踪 + 数据本地持久化
> **协议权威源**：[01-标准层/07-产物质量硬指标库.md](../../01-标准层/07-产物质量硬指标库.md)

---

## 文件清单

| 文件 | 用途 |
|------|------|
| `02-Loop度量看板.html` | 单文件 HTML 看板（双击即开，无任何外部依赖） |

---

## 38 条 KPI 来源

来自 `03-执行层/01-工种Playbook/` 第 7 节，按 6 工种分组：

| 工种 | KPI 数 | 代表指标 |
|------|--------|---------|
| 售前 | 8 | 需求覆盖率 / 方案章完整度 / 投标评分项覆盖 |
| 产品 | 6 | PRD 12 部分完整度 / Part7 五维达标率 / AC 可测试率 |
| 研发 | 6 | 核心代码单测覆盖 / Lint Critical / P0/P1 缺陷 |
| 测试 | 6 | 用例通过率 / P0/P1 缺陷 / 缺陷逃逸率 |
| 交付 | 6 | 验收通过率 / 客户满意度 / 一次部署成功 |
| PM | 6 | 计划偏差率 / 返工次数 / 质量门禁通过率 |

每条 KPI 标注：**目标值 / 类型（百分比/计数/评分/布尔/工时）/ 方向（越高越好 or 越低越好）**，看板按目标自动着色：
- 绿色 = 达标
- 橙色 = 临界
- 红色 = 未达标

---

## 快速上手

### 1. 打开看板

双击 `02-Loop度量看板.html`，浏览器（Chrome/Edge/Firefox）直接打开，无网络依赖。

首次打开会自动生成 1 个「示例项目（参考）」DEMO-001，预填本月随机达标数据。

### 2. 新增项目

右上角「+ 新增项目」→ 填写编码、名称、阶段、状态。

### 3. 录入 KPI

- 切换「项目」「时间周期（YYYY-MM）」
- 点击任一 KPI 卡片 → 弹窗编辑数值 + 备注
- 颜色实时变化（绿/橙/红）

### 4. 多项目横向对比

工具栏「对比模式」切换为「多项目横向对比」→ 每张 KPI 卡片变为表格，列出所有项目的当期数值。

### 5. 时间趋势

切到任一工种 Tab，底部自动渲染**达标率趋势图**（SVG 折线 + 面积），随历史周期数累积。

### 6. 数据备份/迁移

- **导出 CSV**：右上角「导出 CSV」→ 文件名 `loop-dashboard-{周期}-{时间戳}.csv`，含 BOM 兼容 Excel 中文。
- **导入 CSV**：右上角「导入」选择 CSV → 合并到现有数据（不覆盖，按 项目+周期+KPI 合并）。

---

## 与 N 节点 / QG 集成

| KPI 组 | 关联 N 节点 | 关联 QG |
|--------|----------|---------|
| 售前 | D 阶段 | QG-D（D→S） |
| 产品 | S | QG-D（D→S） |
| 研发 | 跨阶段 | QG-S（S→B）/ QG-B（B→Ship） |
| 测试 | Ship 测试 | QG-B（B→Ship）/ QG-Ship（Ship→Ship） |
| 交付 | Ship 部署验收 | QG-Ship（Ship→Ship）/ QG-Ship（Ship→Ship） |
| PM | 全程 全程 | QG-D/S/B/Ship 全程 |

详见 [01-标准层/03-质量门禁QG-D/S/B/Ship.md](../../01-标准层/03-质量门禁QG-D/S/B/Ship.md)。

---

## 数据存储

- **位置**：浏览器 `localStorage`（key: `loop_dashboard_v1`）
- **特性**：刷新不丢失；清浏览器数据会丢失
- **建议**：每周/每月定期「导出 CSV」备份；项目结项时归档到 `项目文档/{项目}/04-管理阶段/loop-kpi-{周期}.csv`

---

## 与团队工程素养盘点工具配合

| 工具 | 用途 | 时机 |
|------|------|------|
| 素养盘点工具 | 评估团队**能力**当前档位（A/B/C） | 项目启动 / 季度复评 |
| Loop 度量看板 | 追踪项目**实际产出** KPI | 项目执行中持续 |

**联动逻辑**：素养盘点定 A 档（成熟团队）→ 看板可作为自动化看板，PM 周例会快速 review；
素养盘点定 C 档（新手团队）→ 看板数据需 PM 逐条人工复核。

---

## FAQ

**Q: 数据能否多端同步？**
A: 本工具纯本地存储，不支持多端同步。如需多人共享，导出 CSV 通过项目 wiki / Git 仓库协作。

**Q: 如何自定义 KPI？**
A: 编辑 HTML 内 `KPI_GROUPS` 常量，按现有结构增删条目。`id` 必须全局唯一。

**Q: 浏览器兼容性？**
A: 原生 HTML + CSS + JS（无 CDN、无框架），支持 Chrome 80+ / Edge / Firefox / Safari。IE 不支持。

**Q: 看板能否嵌入 Confluence/飞书？**
A: HTML 文件可直接以附件形式上传，或嵌入 iframe。CSV 数据可粘贴到飞书表格协作。

---

## 版本记录

| 版本 | 日期 | 修订 |
|------|------|------|
| v1.0 | 2026-06-30 | 初始版本：38 KPI + 多项目对比 + 趋势图 + CSV 导入导出 |

---

*本看板是 Loop Engineering 度量体系的可视化实现。配合 `04-工具层/01-团队工程素养盘点工具.html` + `04-工具层/04-交接契约引用工具/` 一起使用。*
