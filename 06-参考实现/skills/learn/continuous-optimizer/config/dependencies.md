# 依赖配置

## 核心依赖
- **07-project-copilot**：提供历史项目数据、WBS、进度/质量/成本度量
  - 必须可用，否则无数据可分析

## 增强依赖
- **docx**：生成专业的 Word 格式改进报告
  - 可选，不可用时降级为 Markdown 输出

## 依赖检查
启动时确认可获取至少 3 个历史项目的度量数据。
数据不足时仍可执行分析，但标注"样本不足，结论需谨慎"。
